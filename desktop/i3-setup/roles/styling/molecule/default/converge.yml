---
- name: Converge
  hosts: all
  become: true
  vars_files:
    - vars/test-vars.yml
  pre_tasks:
    - name: Install xrdb and xvfb for Xresources reload
      ansible.builtin.apt:
        name:
          - x11-xserver-utils
          - xvfb
        state: present
        update_cache: true

    - name: Start virtual framebuffer
      ansible.builtin.shell: "Xvfb :99 -screen 0 1024x768x24 &"
      changed_when: true

    - name: Set DISPLAY environment variable
      ansible.builtin.lineinfile:
        path: /etc/environment
        line: "DISPLAY=:99"
        create: true
        mode: "0644"

    - name: Create mock i3-msg command
      ansible.builtin.copy:
        content: |
          #!/bin/bash
          echo "mock i3-msg: $@"
          exit 0
        dest: /usr/local/bin/i3-msg
        mode: "0755"

    - name: Create mock dotfiles directory structure
      ansible.builtin.file:
        path: "{{ item }}"
        state: directory
        mode: "0755"
      loop:
        - /tmp/test-dotfiles
        - /tmp/test-dotfiles/.config/i3
        - /tmp/test-dotfiles/.config/kitty

    - name: Create mock .Xresources
      ansible.builtin.copy:
        content: |
          #define green  #a7c080
          #define bg0    #2e383c
          #define fg     #d3c6aa
        dest: /tmp/test-dotfiles/.Xresources
        mode: "0644"

    - name: Create mock i3 config
      ansible.builtin.copy:
        content: |
          gaps inner 10
          gaps outer 5
          default_border pixel 2
          default_floating_border pixel 2
        dest: /tmp/test-dotfiles/.config/i3/config
        mode: "0644"

    - name: Create mock kitty.conf
      ansible.builtin.copy:
        content: |
          font_size 11
        dest: /tmp/test-dotfiles/.config/kitty/kitty.conf
        mode: "0644"
  environment:
    DISPLAY: ":99"
  roles:
    - role: styling
