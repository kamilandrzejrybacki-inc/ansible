---
# ArgoCD MCP Server configuration
argocd_base_url: "https://argocd.kamilandrzejrybacki.dpdns.org"
argocd_mcp_package: "argocd-mcp"

# Claude Code config
claude_mcp_scope: "user"

# Vault
# Issuer-keyed tree (VAULT-TAXONOMY.md): argocd/mcp#token. base_url is config (argocd_base_url above).
vault_argocd_mcp_secret_path: "secret/data/homelab/v2/argocd/mcp"
