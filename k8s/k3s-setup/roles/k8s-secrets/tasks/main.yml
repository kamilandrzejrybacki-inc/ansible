---
# Only Secrets that ESO cannot deliver: restricted Vault material and files that live on the
# control machine. Everything else: argocd-apps/secrets/eso (see VAULT-TAXONOMY.md).
- name: Create vault-mcp-token (restricted)
  ansible.builtin.include_tasks: mcp-servers.yml
  tags: [mcp-servers, secrets]

- name: Create Hermes codex-auth
  ansible.builtin.include_tasks: hermes.yml
  tags: [hermes, secrets]

- name: Create gnhf-claude-auth
  ansible.builtin.include_tasks: gnhf-bot.yml
  tags: [gnhf-bot, secrets]
